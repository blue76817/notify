<div id="events">
  <% @events.each do |event| %>
    <div class="event">
      <h2 class="name">
        <%= link_to event.name, event %>
      </h2>
      <p class="date">
        Published at
        <%= time_ago_in_words(event.created_at) %>
      </p>
    </div>

  <% end %>

</div>
